${module.name}=CAS Subsystem
${module.name}.add=Adds CAS subsystem
${module.name}.remove=Removes CAS subsystem

${module.name}.authentication=Authentication Configuration
${module.name}.authentication.name=Name of the authentication resource
${module.name}.authentication.add=Adds an authentication resource
${module.name}.authentication.remove=Removes an authentication resource
${module.name}.authentication.protocol=Specifies the protocol to use
${module.name}.authentication.service-url=Specifies a URL for this container
${module.name}.authentication.server-url=Specifies a URL for the CAS server
${module.name}.authentication.proxy-callback-url=Specifies a URL for CAS proxy callback
${module.name}.authentication.accept-any-proxy=Flag indicating whether any proxy is acceptable
${module.name}.authentication.allow-empty-proxy-chain=Flag indicating whether an empty proxy chain should be allowed
${module.name}.authentication.renew=Flag indicating whether user must re-authenticate
${module.name}.authentication.allowed-proxy-chain=An allowed proxy chain
${module.name}.authentication.allowed-proxy-chain.add=Adds an allowed proxy chain
${module.name}.authentication.allowed-proxy-chain.remove=Removes an allowed proxy chain
${module.name}.authentication.allowed-proxy-chain.proxies=List of allowed proxy URLs
${module.name}.authentication.allowed-proxy-chain.proxies.proxy=Proxy URL

${module.name}.authorization=Authorization Configuration
${module.name}.authorization.name=Name of the authorization resource
${module.name}.authorization.add=Adds an authorization resource
${module.name}.authorization.remove=Removes an authorization resource

${module.name}.authorization.ldap=LDAP authorization resource
${module.name}.authorization.ldap.add=Adds an LDAP authorization resource
${module.name}.authorization.ldap.remove=Adds an LDAP authorization resource
${module.name}.authorization.ldap.user-search=LDAP user search resource
${module.name}.authorization.ldap.user-search.add=Adds a user search resource
${module.name}.authorization.ldap.user-search.remove=Removes a user search resource
${module.name}.authorization.ldap.user-search.base=Base DN for the search
${module.name}.authorization.ldap.user-search.filter=Search filter expression
${module.name}.authorization.ldap.user-search.scope=Search scope
${module.name}.authorization.ldap.user-search.role-attributes=List of attributes \
  containing role names
${module.name}.authorization.ldap.user-search.role-attributes.role-attribute=\
  Specifies a role name
${module.name}.authorization.ldap.user-search.role-attributes.role-attribute.name=\
  A role name
${module.name}.authorization.ldap.group-search=LDAP group search resource
${module.name}.authorization.ldap.group-search.add=Adds a group search resource
${module.name}.authorization.ldap.group-search.remove=Removes a group search resource
${module.name}.authorization.ldap.group-search.base=Base DN for the search
${module.name}.authorization.ldap.group-search.filter=Search filter expression
${module.name}.authorization.ldap.group-search.scope=Search scope
${module.name}.authorization.ldap.group-search.role-attributes=List of attributes \
  containing role names
${module.name}.authorization.ldap.group-search.role-attributes.role-attribute=\
  Specifies a role name
${module.name}.authorization.ldap.group-search.role-attributes.role-attribute.name=\
  A role name
${module.name}.authorization.ldap.group-search.user-member-attribute=Name of attribute containing a user
${module.name}.authorization.ldap.group-search.user-member-type=Data type of user attribute (distinguished-name or simple-name)
${module.name}.authorization.ldap.group-search.group-member-attribute=Name of attribute containing a nested group
${module.name}.authorization.ldap.group-search.group-member-type=Data type of group attribute (distinguished-name or simple-name)

${module.name}.authorization.properties=Properties authorization resource
${module.name}.authorization.properties.add=Adds a properties authorization resource
${module.name}.authorization.properties.remove=Removes a properties authorization resource
${module.name}.authorization.properties.path=Path to the file containing users and roles
${module.name}.authorization.properties.relative-to=Indicates what the file is relative to some other location



