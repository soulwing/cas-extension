${subsystem.name}=CAS Subsystem
${subsystem.name}.add=Adds CAS subsystem
${subsystem.name}.remove=Removes CAS subsystem

${subsystem.name}.cas-profile=Configuration profile
${subsystem.name}.cas-profile.name=Name of the profile resource
${subsystem.name}.cas-profile.add=Adds a configuration profile
${subsystem.name}.cas-profile.remove=Removes a configuration profile
${subsystem.name}.cas-profile.protocol=Specifies the protocol to use
${subsystem.name}.cas-profile.encoding=Specifies a character encoding
${subsystem.name}.cas-profile.service-url=Specifies the URL for this container
${subsystem.name}.cas-profile.server-url=Specifies the URL for the CAS server
${subsystem.name}.cas-profile.proxy-callback-enabled=Flag indicating whether a proxy granting ticket callback \
  should be requested with each ticket validation
${subsystem.name}.cas-profile.proxy-callback-path=Specifies a context-relative path for proxy callbacks
${subsystem.name}.cas-profile.accept-any-proxy=Flag indicating whether any proxy is acceptable
${subsystem.name}.cas-profile.allow-empty-proxy-chain=Flag indicating whether an empty proxy chain should be allowed
${subsystem.name}.cas-profile.original-request-path-header=Name of an HTTP header containing the original request path when running behind a reverse proxy
${subsystem.name}.cas-profile.renew=Flag indicating whether user must re-authenticate
${subsystem.name}.cas-profile.clock-skew-tolerance=Clock skew tolerance in milliseconds
${subsystem.name}.cas-profile.post-auth-redirect=Flag indicating whether a redirect \
  should be sent after successful authentication to remove CAS parameters from URL
${subsystem.name}.cas-profile.security-realm=Name of a security realm that will be used \
  to obtain a truststore and/or keystore for SSL/TLS negotiation with the CAS server
${subsystem.name}.cas-profile.hostname-verifier=Host (name) verifier for the CAS server
${subsystem.name}.cas-profile.hostname-verifier.add=Adds a host verifier
${subsystem.name}.cas-profile.hostname-verifier.remove=Removes a host verifier
${subsystem.name}.cas-profile.hostname-verifier.hosts=Allowed hostnames or patterns
${subsystem.name}.cas-profile.hostname-verifier.hosts.host=Allowed hostname or pattern
${subsystem.name}.cas-profile.allowed-proxy-chain=An allowed proxy chain
${subsystem.name}.cas-profile.allowed-proxy-chain.add=Adds an allowed proxy chain
${subsystem.name}.cas-profile.allowed-proxy-chain.remove=Removes an allowed proxy chain
${subsystem.name}.cas-profile.allowed-proxy-chain.proxies=List of allowed proxy URLs
${subsystem.name}.cas-profile.allowed-proxy-chain.proxies.proxy=Proxy URL
${subsystem.name}.cas-profile.cas-status-cookie-enabled=Flag indicator whether to use a \
  cookie to track the login attempt count
${subsystem.name}.cas-profile.attribute-transform=A transform to apply to an identity attribute
${subsystem.name}.cas-profile.attribute-transform.add=Adds an identity attribute transform definition
${subsystem.name}.cas-profile.attribute-transform.remove=Removes an an identity attribute transform definition
${subsystem.name}.cas-profile.attribute-transform.transformer=A transformer for values of a named identity attribute
${subsystem.name}.cas-profile.attribute-transform.transformer.add=Adds a identity attribute transformer
${subsystem.name}.cas-profile.attribute-transform.transformer.remove=Removes an identity attribute transformer
${subsystem.name}.cas-profile.attribute-transform.transformer.code=Fully-qualified class name (or short name) of the transformer implementation
${subsystem.name}.cas-profile.attribute-transform.transformer.module=Module that contains the transformer class
${subsystem.name}.cas-profile.attribute-transform.transformer.options=Transformer options
