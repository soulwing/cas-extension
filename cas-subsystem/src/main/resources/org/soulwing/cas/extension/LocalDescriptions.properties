${subsystem.name}=CAS Subsystem
${subsystem.name}.add=Adds CAS subsystem
${subsystem.name}.remove=Removes CAS subsystem

${subsystem.name}.profile=Configuration profile
${subsystem.name}.profile.name=Name of the profile resource
${subsystem.name}.profile.add=Adds a configuration profile
${subsystem.name}.profile.remove=Removes a configuration profile
${subsystem.name}.profile.protocol=Specifies the protocol to use
${subsystem.name}.profile.encoding=Specifies a character encoding
${subsystem.name}.profile.service-url=Specifies the URL for this container
${subsystem.name}.profile.server-url=Specifies the URL for the CAS server
${subsystem.name}.profile.proxy-callback-enabled=Flag indicating whether a proxy granting ticket callback \
  should be requested with each ticket validation
${subsystem.name}.profile.proxy-callback-path=Specifies a context-relative path for proxy callbacks
${subsystem.name}.profile.accept-any-proxy=Flag indicating whether any proxy is acceptable
${subsystem.name}.profile.allow-empty-proxy-chain=Flag indicating whether an empty proxy chain should be allowed
${subsystem.name}.profile.renew=Flag indicating whether user must re-authenticate
${subsystem.name}.profile.clock-skew-tolerance=Clock skew tolerance in milliseconds
${subsystem.name}.profile.post-auth-redirect=Flag indicating whether a redirect \
  should be sent after successful authentication to remove CAS parameters from URL
${subsystem.name}.profile.security-realm=Name of a security realm that will be used \
  to obtain a truststore and/or keystore for SSL/TLS negotiation with the CAS server
${subsystem.name}.profile.hostname-verifier=Host (name) verifier for the CAS server
${subsystem.name}.profile.hostname-verifier.add=Adds a host verifier
${subsystem.name}.profile.hostname-verifier.remove=Removes a host verifier
${subsystem.name}.profile.hostname-verifier.hosts=Allowed hostnames or patterns
${subsystem.name}.profile.hostname-verifier.hosts.host=Allowed hostname or pattern
${subsystem.name}.profile.allowed-proxy-chain=An allowed proxy chain
${subsystem.name}.profile.allowed-proxy-chain.add=Adds an allowed proxy chain
${subsystem.name}.profile.allowed-proxy-chain.remove=Removes an allowed proxy chain
${subsystem.name}.profile.allowed-proxy-chain.proxies=List of allowed proxy URLs
${subsystem.name}.profile.allowed-proxy-chain.proxies.proxy=Proxy URL
${subsystem.name}.profile.attribute-transform=A transform to apply to an identity attribute
${subsystem.name}.profile.attribute-transform.add=Adds an identity attribute transform definition
${subsystem.name}.profile.attribute-transform.remove=Removes an an identity attribute transform definition
${subsystem.name}.profile.attribute-transform.transformer=A transformer for values of a named identity attribute
${subsystem.name}.profile.attribute-transform.transformer.add=Adds a identity attribute transformer
${subsystem.name}.profile.attribute-transform.transformer.remove=Removes an identity attribute transformer
${subsystem.name}.profile.attribute-transform.transformer.code=Fully-qualified class name (or short name) of the transformer implementation
${subsystem.name}.profile.attribute-transform.transformer.module=Module that contains the transformer class
${subsystem.name}.profile.attribute-transform.transformer.options=Transformer options
