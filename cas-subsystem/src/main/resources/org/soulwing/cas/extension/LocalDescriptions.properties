${module.name}=CAS Subsystem
${module.name}.add=Adds CAS subsystem
${module.name}.remove=Removes CAS subsystem

${module.name}.profile=Configuration profile
${module.name}.profile.name=Name of the profile resource
${module.name}.profile.add=Adds a configuration profile
${module.name}.profile.remove=Removes a configuration profile
${module.name}.profile.protocol=Specifies the protocol to use
${module.name}.profile.service-url=Specifies the URL for this container
${module.name}.profile.server-url=Specifies the URL for the CAS server
${module.name}.profile.proxy-callback-url=Specifies a URL for CAS proxy callback
${module.name}.profile.accept-any-proxy=Flag indicating whether any proxy is acceptable
${module.name}.profile.allow-empty-proxy-chain=Flag indicating whether an empty proxy chain should be allowed
${module.name}.profile.renew=Flag indicating whether user must re-authenticate
${module.name}.profile.clock-skew-tolerance=Clock skew tolerance in milliseconds
${module.name}.profile.allowed-proxy-chain=An allowed proxy chain
${module.name}.profile.allowed-proxy-chain.add=Adds an allowed proxy chain
${module.name}.profile.allowed-proxy-chain.remove=Removes an allowed proxy chain
${module.name}.profile.allowed-proxy-chain.proxies=List of allowed proxy URLs
${module.name}.profile.allowed-proxy-chain.proxies.proxy=Proxy URL


